import React from 'react';
import classes from './IconTable.module.scss';

const IconTable = (props) => {
    return (
        <div className={classes.iconTable}>
            <div className={classes.line} />
            <div className={classes.line} />
        </div>
    )
}

export default IconTable;
